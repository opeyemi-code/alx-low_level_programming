#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * _abs - Entry point
 * @x: takes an integer as arguement
 * this program prints out the absolute value of any number
 * Return: Always 0 (Success)
 */

int _abs(int x)
{
	return (abs(x));
}
