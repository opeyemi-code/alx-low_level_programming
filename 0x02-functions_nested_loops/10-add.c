#include "main.h"
#include <stdio.h>
/**
 * add - Entry point
 * @a: receives interger as an argument
 * @b: receives interger as an argument
 * this function adds two intergers together
 * Return: Always 0 (Success)
 */
int add(int a, int b)
{
	return (a + b);
}
