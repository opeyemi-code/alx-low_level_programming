#include <string.h>
#include "main.h"

/**
 * _strlen - Entry point
 * @s: takes a pointer argument that points to a string
 * this program returns the lenght of a string
 * Return: length
 */

int _strlen(char *s)
{
	return (strlen(s));
}
