#include <stdio.h>
#include "main.h"

/**
 * _puts - Entry point
 * @str: receives a pointer that points to a string
 * this prograam prints a string to the stdout
 * Return: str
 */

void _puts(char *str)
{
	puts(str);
}
