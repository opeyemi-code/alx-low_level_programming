#include <stdio.h>
#include "main.h"
#include <string.h>

/**
 * _strcpy - Entry point
 * @dest: receives a string
 * @src: receives a string
 * Return: a new String
 */

char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
