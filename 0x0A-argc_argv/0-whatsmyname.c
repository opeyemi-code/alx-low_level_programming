#include <stdio.h>

/**
 * main - Entry point
 * @argc: an integer
 * @argv: an array of pointer to string
 *
 * Return: an integer
 */

int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
