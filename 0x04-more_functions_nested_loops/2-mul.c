#include "main.h"

/**
 * mul - Entry point
 * @a: receives an integer
 * @b: receives an interger
 * returns the product of a and b
 * Return: Always 0 (Success)
 */

int mul(int a, int b)
{
	return (a * b);
}
